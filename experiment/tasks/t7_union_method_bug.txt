A user reports: "When I write `x = condition ? [1, 2] : 'hello'` and then call `x.class`, TypeGuessr shows `untyped` instead of `Class`. It seems like method calls on variables with union types always return untyped." Diagnose this bug by tracing the code path from variable assignment through type inference of the method call. Identify the root cause, list all files that need modification, and propose a fix plan. Do not write code.